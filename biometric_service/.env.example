# Biometric Service Environment Configuration

# Server Configuration
HOST=0.0.0.0
PORT=8000
RELOAD=true

# CORS Configuration (comma-separated origins)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:8080

# Face Recognition Settings
FACE_MATCH_TOLERANCE=0.45
MIN_BLUR_SCORE=100
MIN_BRIGHTNESS=50
MAX_BRIGHTNESS=200

# Security
# API_KEY=your-secret-api-key-here  # Uncomment to enable API key authentication

# Logging
LOG_LEVEL=info

# Data Storage
FACES_DIR=data/faces
FINGERPRINTS_DIR=data/fingerprints

# Optional: Database connection (for future use)
# DATABASE_URL=postgresql://user:password@localhost/biometric_db
