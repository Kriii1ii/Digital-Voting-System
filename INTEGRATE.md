@workspace /fixrebase Complete the git rebase by resolving all remaining conflicts in these files: predictionController.js, Election.js, package.json, prediction.js, seedVoters.js, server.js. Use the merged versions that maintain AI prediction logic, socket.io emits, biometric authentication, and CommonJS style. Stage all resolved files and run git rebase --continue with an appropriate commit message about integrating AI, biometric, and real-time features.